using UnityEngine;

namespace MundoMental.VR.Combat
{
    /// <summary>Marca un collider como suelo transitable (generado por <see cref="WalkableMapGroundSetup"/>).</summary>
    [DisallowMultipleComponent]
    public sealed class WalkableGroundMarker : MonoBehaviour
    {
    }
}
